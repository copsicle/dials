import React from "react"
import ReactDOM from "react-dom"
import DialApp from "./components/DialApp"

// Render single application in root
ReactDOM.render(<DialApp />, document.getElementById("root"))